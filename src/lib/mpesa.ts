function env(name:string){const v=process.env[name];if(!v)throw new Error(`Missing ${name}`);return v;}
function base(){return process.env.MPESA_ENVIRONMENT==="production"?"https://api.safaricom.co.ke":"https://sandbox.safaricom.co.ke";}
export function normalizeKenyanPhone(value:string){const x=value.replace(/[\s-]/g,"");if(/^254[17]\d{8}$/.test(x))return x;if(/^0[17]\d{8}$/.test(x))return `254${x.slice(1)}`;if(/^\+254[17]\d{8}$/.test(x))return x.slice(1);throw new Error("Enter a valid Kenyan mobile number.");}
export async function initiateMpesaStk(input:{amount:number;phone:string;reference:string;description:string}){const shortcode=env("MPESA_SHORTCODE"),passkey=env("MPESA_PASSKEY"),callback=env("MPESA_CALLBACK_URL");const timestamp=new Date().toISOString().replace(/[-:TZ.]/g,"").slice(0,14);const password=Buffer.from(`${shortcode}${passkey}${timestamp}`).toString("base64");const auth=Buffer.from(`${env("MPESA_CONSUMER_KEY")}:${env("MPESA_CONSUMER_SECRET")}`).toString("base64");const tokenRes=await fetch(`${base()}/oauth/v1/generate?grant_type=client_credentials`,{headers:{Authorization:`Basic ${auth}`},cache:"no-store"});const token=await tokenRes.json();if(!tokenRes.ok||!token.access_token)throw new Error("Unable to authenticate with M-PESA.");const phone=normalizeKenyanPhone(input.phone);const res=await fetch(`${base()}/mpesa/stkpush/v1/processrequest`,{method:"POST",headers:{Authorization:`Bearer ${token.access_token}`,"Content-Type":"application/json"},body:JSON.stringify({BusinessShortCode:shortcode,Password:password,Timestamp:timestamp,TransactionType:process.env.MPESA_TRANSACTION_TYPE||"CustomerPayBillOnline",Amount:Math.max(1,Math.round(input.amount)),PartyA:phone,PartyB:shortcode,PhoneNumber:phone,CallBackURL:callback,AccountReference:input.reference.slice(0,12),TransactionDesc:input.description.slice(0,13)}),cache:"no-store"});const data=await res.json();if(!res.ok||data.ResponseCode!=="0")throw new Error(data.errorMessage||data.ResponseDescription||"M-PESA request failed.");return data;}
